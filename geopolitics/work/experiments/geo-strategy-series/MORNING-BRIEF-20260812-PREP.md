# Morning Brief Preparation: 2026-08-12

Status: `prepared-for-live-receipt`

## Contract

- Mode: `scan`
- Geography: `global`
- Output: `five-minute-selective-global-update`
- Observation window: the 24 hours ending at tomorrow's explicit UTC as-of time
- Selection: up to four material developments plus one outlier or visibility gap
- Receipt schema: `2.1`
- Canonical output: withheld until the fresh receipt is complete and validated

## Baseline Readiness

- Valid judgment files in the 30-day lookback: `10`
- Accountable open forecast rows available: `13`
- Overdue accountable open forecasts requiring careful pressure labeling: `6`
- Baseline paths and SHA-256 values: generated by the renderer's current-state
  baseline check at render time; do not hand-copy stale values into the receipt.

The overdue rows are not automatically resolved by Morning Brief. They should
be labeled as due-only, pressured, or unaffected only after fresh observations
are recovered and the receipt supports that label.

## Tomorrow's Work Order

1. Set the exact RFC3339 UTC as-of time.
2. Freeze the preceding 24-hour window.
3. Recover fresh upstream sources before retaining observations.
4. Review at least three geographies, two domains, three upstream providers,
   and three independent lineage roots.
5. Select no more than four material developments and exactly one outlier or gap.
6. Link each selected development to the inherited judgment or forecast it
   pressures, using `strengthens`, `weakens`, or `complicates`.
7. Run Reality Check only for exact atomic matches; never create or resolve
   lattice claims during this workflow.
8. Validate the completed receipt, then render the paired internal artifact.

## Stop Conditions

Fail closed and generate nothing if timestamps, upstream recovery, baseline
hashes, or minimum coverage are incomplete. If retrieval is healthy but no
candidate clears the threshold, generate a `no material change` brief with the
strongest visibility gap.

## Boundary

This preparation does not create a morning brief, alter daily synthesis,
modify forecasts, create verification packets, or publish anything.
