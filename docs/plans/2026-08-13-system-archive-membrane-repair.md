
# Repair the Geopoliticsâ€“Singularity Membrane

Status: `approved-plan-candidate`

## Summary

Repair the boundary in this order:

`restore control plane â†’ admit Moonshots correctly â†’ verify canonical/replica â†’ withdraw geopolitical membership â†’ prevent recurrence`

No surviving source bytes should be deleted. The existing Archive Repair
command is unsuitable for the membership change because it governs bounded
metadata/body repair, not collection reassignment or voice-shelf deletion.

## 1. Restore the repository control plane

Recover, review, and integrate the already-completed restoration work rather
than recreating it:

- `e7fb744 Restore autobiographical source archive access`
- `5cca048 Admit Nate Herk browser automation source`

These commits restore:

- Moonshots at 31 active records.
- Nate Herk at 21 active records.
- Nate B. Jones at 23 active records.
- Explicit-only and hydration-disabled policies.
- Private configuration resolution.
- `doctor` and exact retrieval interfaces.
- Autobiographical-source governance.

Perform the integration in a clean Narrative Systems worktree based on the
current branch. Resolve conflicts by preserving the newer counts and source
commits from `5cca048`.

Acceptance:

- Repository collections match canonical active counts: Innermost Loop 193,
  Moonshots 31, Nate Herk 21, Nate B. Jones 23.
- CLI can explicitly search and retrieve every collection.
- A clean process discovers
  `C:\private\narrative-system-archive-config.json` without manually supplied
  environment variables.
- `doctor` fails on registry/catalog drift.
- No canonical archive mutation occurs during this phase.

## 2. Preserve and admit the new Moonshots episode correctly

Before changing membership, bind the supplied source:

- Original attachment: 140,575 bytes
- SHA-256: `b6cf3bbd86c1df1fc706bb7930c9963988b054156db33d482819668b33884ba9`
- Existing geopolitical wrapper: 141,806 bytes
- SHA-256: `96fd367c50c16470d9d1137ba91a5f33fd619327284fc0386224c40c7c6dac3e`

Because `C:\dev\anyang-intelligence` has an unusable empty `.git` directory,
do not repair or overwrite it. Create a fresh clean Anyang worktree at:

`C:\private\anyang-moonshots-uoGnH0REG7A`

Admit the original attachment into the Anyang Moonshots corpus as:

- one preserved transcript;
- one conservative source note;
- no factual verification or derived analysis unless separately requested.

Update the Anyang registry, validate it, commit it, and record the new pinned
source commit. External Anyang mutation, commit, and push remain separate
authorization boundaries.

## 3. Advance the System Archive Moonshots registry

In Narrative Systems:

- Update the Moonshots registry to the new Anyang commit.
- Preserve `record_identity_commit` so existing record identities remain
  stable.
- Add the transcript and source-note records.
- Increase `expected_records` from 31 to 33.
- Preserve:
  - `explicit-only`;
  - hydration disabled;
  - `may_promote: false`;
  - `may_quote: false`;
  - `may_republish: false`;
  - `may_route_to_customer: false`.
- Add only collection-internal `derived_from` lineage from source note to
  transcript.

Run ingestion first with `--check`. Actual canonical ingestion requires a
separate explicit command.

Acceptance:

- Moonshots has 33 active records.
- The new transcript is retrievable only when `--collection moonshots` is
  named.
- Default search and context compilation exclude it.
- No cross-collection provenance edge is created.
- Canonical and replica fingerprints match after synchronization.

## 4. Withdraw the geopolitical misroute

Only after successful Moonshots retrieval:

- Remove `youtube:uoGnH0REG7A` from the Narrative Geopolitics manifest.
- Remove its Mostaque voice-index route.
- Remove the provisional Mostaque voice shelf only if it contains no other
  legitimate geopolitical sources.
- Preserve the ignored geopolitical wrapper for 30 days as a recovery source
  with a migration receipt; do not delete it during repair.
- Record the old and new collection identities, source hashes, Anyang commit,
  canonical object ID, and replica fingerprint.

Then run a geopolitical ingestion dry-run and prove that the source is absent
from its planned inventory.

## 5. Prevent recurrence

Harden the intake boundary:

- Detect known singularity families from explicit operator labels,
  show/channel identity, and registered source-family rules.
- If a source is identified as Moonshots, Innermost Loop, Nate Herk, or Nate B.
  Jones, the Narrative Geopolitics intake helper must fail closed before
  writing.
- Return the correct singularity admission route instead of inventing a
  geopolitical voice lane.
- Require an explicit, reviewed cross-domain evidence packet before any
  singularity source can enter geopolitical membership.
- A topical overlapâ€”AI policy, compute finance, war, state powerâ€”must not itself
  transfer collection authority.

Add tests for:

- bare `intake moonshots` refusing geopolitical landing;
- explicit singularity admission preserving external-corpus routing;
- catalog/registry count drift;
- catalog-only collections;
- default-search exclusion;
- cross-collection edge rejection;
- attempted geopolitical ingestion of a record marked
  `routing_note: Misrouted intake`.

## Final acceptance

The repair is complete when:

- all four singularity corpora are registered and explicitly retrievable;
- the new episode exists only under Moonshots membership;
- geopolitical ingestion cannot rediscover it;
- canonical and replica verification pass;
- no source bytes were deleted;
- every migration step has a digest-bound receipt;
- no authority, truth status, identity status, or publication right crossed
  with the content.

## Boundaries

- This document is a repair plan, not execution authority.
- Narrative Systems and Anyang Intelligence remain separate mutation, commit,
  and push boundaries.
- Canonical ingestion and replica synchronization require explicit authority
  after dry-run review.
- Retirement or deletion of recovery sources remains a later destructive-action
  boundary.
