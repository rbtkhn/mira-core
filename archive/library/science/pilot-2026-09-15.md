# Science pilot admission - September 15, 2026

Four papers attempted, downloaded, inspected, and admitted. None deferred,
rejected, or metadata-only. Four original PDFs and four searchable text copies
are private local payloads under the existing Library text root.

The [machine receipt](pilot-2026-09-15.json) records exact URLs, versions via the
registry, hashes, sizes, and body IDs. Registry representation, physical presence,
and hashes passed. Science has no era requirement and no historical seal claim.

Original title pages were visually inspected; every PDF page yielded text.
The derivatives retain page markers but have partial-work coverage because
figures, equations, and table layout require the originals. Reading state is
saved; acquisition does not establish full reading or assessment.

The citation-capacity PDF was acquired from arXiv's official export endpoint
after the main PDF endpoint returned HTTP 406. Existing verified files were
reused. No different edition was substituted.

Validation: registry and generated indexes pass; 31 focused Library tests pass.
The private text verification checked 806 active bodies without failure. The
23 authorities without active bodies are outside this four-paper pilot.
Existing History source records were preserved exactly. The generated route
index changed only its Library registry fingerprint; route eligibility did not
change. No new cognitive notes, relationships, or routes were admitted.

No staging, commit, push, Archive ingestion, or public source-body publication.
Re-entry: use the Science index to select a paper for reading, or Library Import
for an explicitly selected addition. Do not repeat acquisition merely to resume.

## Checkpoint readiness

The isolated checkpoint also completes the 17 Library note relocations already
named by the committed path resolver and registers the existing Tolstoy and
Homer successor notes. Historical note bodies are preserved, with existing
companion-link corrections. Required Library views are regenerated against
only the checkpoint registry. Unrelated History body additions remain excluded.
The Library, integration and daily-run suite passes 80 tests on that isolated
candidate; registry and generated-view checks pass without a gate exception.
Private PDF/text payloads remain outside Git. This is a local commit checkpoint;
push is not part of this request.
